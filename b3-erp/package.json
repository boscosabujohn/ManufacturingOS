{
  "name": "manufacturing-os-erp",
  "version": "1.0.0",
  "description": "Manufacturing OS ERP Root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"cd backend && pnpm run start:dev\" \"cd frontend && pnpm run dev\"",
    "start": "pnpm run dev",
    "docker:up": "docker-compose up -d postgres redis",
    "setup": "make setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
